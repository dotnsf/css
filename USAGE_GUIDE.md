# CSS - Command-Line Spreadsheet 使い方ガイド

## クイックスタート

### 1. ビルド

```powershell
cd css
.\build.ps1
```

ビルドが成功すると、`publish` フォルダに `css.exe` が生成されます。

### 2. 実行

```cmd
# 新規スプレッドシート
cd publish
css.exe

# CSV ファイルを開く
css.exe sample.csv
```

## 基本操作

### ナビゲーションモード（デフォルト）

| キー | 動作 |
|------|------|
| ↑↓←→ | セル間を移動 |
| h j k l | セル間を移動（Vi ライク） |
| Enter または i | セルを編集モードに |
| : | コマンドモードに入る |
| Page Up/Down | 画面をスクロール |
| Home | 行の先頭に移動 |
| End | 行の末尾に移動 |
| Delete | 現在のセルを削除 |

**Vi ライクなキーバインド:**
- `h` - 左に移動（←と同じ）
- `j` - 下に移動（↓と同じ）
- `k` - 上に移動（↑と同じ）
- `l` - 右に移動（→と同じ）
- `i` - 編集モードに入る（Enter と同じ）

### 編集モード

| キー | 動作 |
|------|------|
| 文字入力 | セルの内容を編集 |
| Enter | 編集を確定 |
| ESC | 編集をキャンセル |
| ←→ | カーソル移動 |
| Backspace | 文字削除 |
| Delete | カーソル位置の文字を削除 |
| Home/End | カーソルを先頭/末尾に |

### コマンドモード

`:` を押してコマンドを入力：

| コマンド | 説明 | 例 |
|----------|------|-----|
| `:w [filename]` | 保存 | `:w data.csv` |
| `:o filename` | 開く | `:o data.csv` |
| `:n` | 新規作成 | `:n` |
| `:width N` | 列幅を変更 | `:width 15` |
| `:q` | 終了 | `:q` |
| `:q!` | 強制終了（未保存でも終了） | `:q!` |
| `:wq [filename]` | 保存して終了 | `:wq` |

## 数式の使い方

### 基本的な演算

```
=1+2          → 3
=10-5         → 5
=3*4          → 12
=20/4         → 5
=(1+2)*3      → 9
```

### セル参照

```
=A1           → A1 の値
=A1+B1        → A1 と B1 の合計
=A1*B2+C3     → A1×B2 + C3
```

### 関数

#### SUM - 合計

```
=SUM(A1:A10)      → A1～A10 の合計
=SUM(A1:B5)       → A1～B5 の全セルの合計
```

#### AVERAGE - 平均

```
=AVERAGE(A1:A10)  → A1～A10 の平均
```

#### MIN - 最小値

```
=MIN(A1:A10)      → A1～A10 の最小値
```

#### MAX - 最大値

```
=MAX(A1:A10)      → A1～A10 の最大値
```

#### IF - 条件分岐

```
=IF(A1>10, "High", "Low")     → A1が10より大きければ "High"
=IF(A1=B1, 1, 0)              → A1とB1が等しければ 1
```

#### COUNT - 数値の個数

```
=COUNT(A1:A10)    → A1～A10 の数値セルの個数
```

#### COUNTA - 空でないセルの個数

```
=COUNTA(A1:A10)   → A1～A10 の空でないセルの個数
```

## 実用例

### 例1: 簡単な計算表

```
     A        B        C
1    100      200      =A1+B1
2    150      250      =A2+B2
3    =SUM(A1:A2)  =SUM(B1:B2)  =SUM(C1:C2)
```

結果：
```
     A        B        C
1    100      200      300
2    150      250      400
3    250      450      700
```

### 例2: 成績表

```
     A          B      C      D
1    Name       Math   Eng    Average
2    Alice      85     90     =AVERAGE(B2:C2)
3    Bob        75     80     =AVERAGE(B3:C3)
4    Charlie    95     88     =AVERAGE(B4:C4)
5    Average    =AVERAGE(B2:B4)  =AVERAGE(C2:C4)  =AVERAGE(D2:D4)
```

### 例3: 条件付き評価

```
     A        B                                    C
1    Score    Grade                                Pass/Fail
2    85       =IF(A2>=90,"A",IF(A2>=80,"B","C"))  =IF(A2>=60,"Pass","Fail")
3    92       =IF(A3>=90,"A",IF(A3>=80,"B","C"))  =IF(A3>=60,"Pass","Fail")
4    55       =IF(A4>=90,"A",IF(A4>=80,"B","C"))  =IF(A4>=60,"Pass","Fail")
```

## ワークフロー例

### 新規スプレッドシートの作成と保存

1. `css.exe` を起動
2. 矢印キーでセルを移動
3. Enter キーでセルを編集
4. データを入力して Enter で確定
5. `:w mydata.csv` で保存
6. `:q` で終了

### 既存ファイルの編集

1. `css.exe mydata.csv` で起動
2. データを編集
3. `:w` で上書き保存
4. `:wq` で保存して終了

### 列幅の調整

1. 幅を変更したい列のセルに移動
2. `:width 20` で列幅を20文字に設定
3. 変更を確認
4. `:w` で保存

## トラブルシューティング

### Q: 数式が計算されない

A: 数式は `=` で始まる必要があります。例：`=A1+B1`

### Q: エラーが表示される

エラーの種類：
- `#DIV/0!` - ゼロ除算
- `#REF!` - 無効なセル参照
- `#NAME?` - 未知の関数名
- `#VALUE!` - 型エラー
- `#CIRCULAR!` - 循環参照
- `#ERROR!` - その他のエラー

### Q: 画面が崩れる

A: ターミナルのサイズを大きくしてください。最小推奨サイズは 80×24 です。

### Q: 日本語が文字化けする

A: コマンドプロンプトで以下を実行：
```cmd
chcp 65001
```

### Q: 保存せずに終了してしまった

A: 未保存の変更がある場合、`:q` では終了できません。`:wq` で保存して終了するか、変更を破棄する場合は新しいファイルを開いてください。

## キーボードショートカット一覧

### ナビゲーション
- `↑↓←→` または `k j h l` - セル移動（Vi ライク）
- `Home` - 行の先頭
- `End` - 行の末尾
- `Page Up/Down` - ページスクロール

### 編集
- `Enter` または `i` - 編集開始
- `Enter`（編集中） - 編集確定
- `ESC` - キャンセル
- `Delete` - セル削除

### コマンド
- `:` - コマンドモード
- `:w` - 保存
- `:o` - 開く
- `:n` - 新規
- `:q` - 終了
- `:q!` - 強制終了
- `:wq` - 保存して終了

## 制限事項

- 最大列数: 702列（A～ZZ）
- 最大行数: 1024行
- 列幅: 3～50文字
- ファイル形式: CSV（UTF-8）のみ

## ヒント

1. **大きなファイル**: 1024行×702列すべてを使用できますが、実際に使用するのは必要な範囲のみにすることをお勧めします。

2. **数式のコピー**: 現在、数式のコピー機能はありません。各セルに個別に入力する必要があります。

3. **バックアップ**: 重要なデータは定期的に保存してください（`:w`）。

4. **パフォーマンス**: 複雑な数式や大量のセルを使用する場合、計算に時間がかかることがあります。

5. **CSV互換性**: 生成されたCSVファイルはExcelや他のスプレッドシートアプリケーションで開くことができます。

## サポート

問題が発生した場合は、以下を確認してください：
- .NET 8.0 Runtime が含まれているか（自己完結型ビルドの場合は不要）
- ファイルパスが正しいか
- ファイルの読み書き権限があるか
- ターミナルのサイズが十分か

---

楽しいスプレッドシート作業を！