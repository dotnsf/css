# CSS - クイックスタートガイド

## 🚀 5分で始める

### 前提条件

- Windows 10/11 (64-bit)
- .NET 8.0 SDK（ビルド時のみ必要）

### ステップ1: ビルド

```powershell
cd css
.\build.ps1
```

ビルドが成功すると、`publish` フォルダに `css.exe` が生成されます。

### ステップ2: 実行

```cmd
cd publish
css.exe
```

### ステップ3: 基本操作を試す

1. **セルに移動**: 矢印キー（↑↓←→）
2. **セルを編集**: Enter キーを押す
3. **値を入力**: 例えば「100」と入力
4. **確定**: Enter キーを押す
5. **次のセルへ**: 右矢印キー
6. **数式を入力**: Enter → 「=A1*2」と入力 → Enter

### ステップ4: ファイルを保存

1. コロンキー（:）を押す
2. 「w test.csv」と入力
3. Enter キーを押す

### ステップ5: 終了

1. コロンキー（:）を押す
2. 「q」と入力
3. Enter キーを押す

## 📝 サンプルを試す

```cmd
cd publish
css.exe ..\sample.csv
```

サンプルファイルには、数式の例が含まれています。

## 🎯 よく使うコマンド

| コマンド | 説明 |
|----------|------|
| `:w filename.csv` | 保存 |
| `:o filename.csv` | 開く |
| `:wq` | 保存して終了 |
| `:q` | 終了 |
| `:width 15` | 列幅を15に設定 |

## 💡 数式の例

```
=A1+B1              # 足し算
=A1*2               # 掛け算
=SUM(A1:A10)        # 合計
=AVERAGE(A1:A10)    # 平均
=IF(A1>10,"大","小") # 条件分岐
```

## 📚 詳細情報

- [README.md](README.md) - 基本的な使い方
- [USAGE_GUIDE.md](USAGE_GUIDE.md) - 詳細なガイド
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - プロジェクト概要

## ❓ トラブルシューティング

### ビルドエラーが出る

.NET 8.0 SDK がインストールされているか確認：

```cmd
dotnet --version
```

### 実行ファイルが見つからない

ビルドが成功したか確認：

```cmd
dir publish\css.exe
```

### 文字化けする

コマンドプロンプトで UTF-8 を設定：

```cmd
chcp 65001
```

---

楽しいスプレッドシート作業を！ 🎉