# 関数リファレンスガイド

このドキュメントでは、CSSスプレッドシートアプリケーションで使用できる全ての関数について説明します。

## 基本的な使い方

関数は `=` で始まる数式内で使用します。例：
```
=SUM(A1:A10)
=AVERAGE(B1:B5)
=IF(C1>100, "合格", "不合格")
```

## 数学・統計関数

### SUM
**説明**: 指定された範囲または値の合計を計算します。

**構文**: `=SUM(範囲1, [範囲2], ...)`

**例**:
```
=SUM(A1:A10)           // A1からA10までの合計
=SUM(A1:A5, B1:B5)     // 複数範囲の合計
=SUM(10, 20, 30)       // 直接値の合計
```

### AVERAGE
**説明**: 指定された範囲または値の平均を計算します。

**構文**: `=AVERAGE(範囲1, [範囲2], ...)`

**例**:
```
=AVERAGE(A1:A10)       // A1からA10までの平均
=AVERAGE(B1:B5, C1:C5) // 複数範囲の平均
```

### MIN
**説明**: 指定された範囲または値の最小値を返します。

**構文**: `=MIN(範囲1, [範囲2], ...)`

**例**:
```
=MIN(A1:A10)           // A1からA10までの最小値
=MIN(10, 20, 5, 30)    // 直接値の最小値
```

### MAX
**説明**: 指定された範囲または値の最大値を返します。

**構文**: `=MAX(範囲1, [範囲2], ...)`

**例**:
```
=MAX(A1:A10)           // A1からA10までの最大値
=MAX(B1:B5, C1:C5)     // 複数範囲の最大値
```

## カウント関数

### COUNT
**説明**: 指定された範囲内の数値セルの個数を数えます。

**構文**: `=COUNT(範囲1, [範囲2], ...)`

**例**:
```
=COUNT(A1:A10)         // A1からA10までの数値セルの個数
=COUNT(A1:A5, B1:B5)   // 複数範囲の数値セルの個数
```

**注意**: 空白セルや文字列は数えません。

### COUNTA
**説明**: 指定された範囲内の空白でないセルの個数を数えます。

**構文**: `=COUNTA(範囲1, [範囲2], ...)`

**例**:
```
=COUNTA(A1:A10)        // A1からA10までの空白でないセルの個数
=COUNTA(A1:C10)        // 複数列の空白でないセルの個数
```

**注意**: 数値、文字列、数式など、何らかの値が入っているセルを全て数えます。

### COUNTIF
**説明**: 指定された範囲内で条件を満たすセルの個数を数えます。

**構文**: `=COUNTIF(範囲, 条件)`

**例**:
```
=COUNTIF(A1:A10, ">50")      // 50より大きい値のセル数
=COUNTIF(A1:A10, "=合格")    // "合格"と等しいセル数
=COUNTIF(B1:B10, ">=100")    // 100以上の値のセル数
=COUNTIF(C1:C10, "<>0")      // 0でない値のセル数
```

**条件の指定方法**:
- `">50"` : 50より大きい
- `">=50"` : 50以上
- `"<50"` : 50より小さい
- `"<=50"` : 50以下
- `"=テキスト"` : テキストと等しい
- `"<>0"` : 0でない

## 条件付き集計関数

### SUMIF
**説明**: 指定された範囲内で条件を満たすセルの合計を計算します。

**構文**: `=SUMIF(条件範囲, 条件, [合計範囲])`

**例**:
```
=SUMIF(A1:A10, ">50")              // A列で50より大きい値の合計
=SUMIF(A1:A10, ">=100", B1:B10)    // A列が100以上の行のB列の合計
=SUMIF(C1:C10, "合格", D1:D10)     // C列が"合格"の行のD列の合計
```

**使用例**:
```
A列（点数）  B列（ボーナス）
80          10
60          5
90          15
40          0

=SUMIF(A1:A4, ">=70", B1:B4)  // 結果: 25 (80点と90点の行のボーナス合計)
```

## 検索関数

### VLOOKUP
**説明**: 表の最初の列で値を検索し、同じ行の指定された列の値を返します。

**構文**: `=VLOOKUP(検索値, 範囲, 列番号, [検索方法])`

**パラメータ**:
- **検索値**: 検索する値
- **範囲**: 検索対象の表範囲（例: A1:D10）
- **列番号**: 返す値がある列の番号（1から始まる）
- **検索方法**: TRUE（近似一致）またはFALSE（完全一致）。省略時はFALSE

**例**:
```
=VLOOKUP(100, A1:C10, 2)           // A列で100を検索し、B列の値を返す
=VLOOKUP("田中", A1:D20, 3, FALSE) // A列で"田中"を検索し、C列の値を返す
```

**使用例**:
```
A列（ID）  B列（名前）  C列（部署）
101       田中        営業
102       佐藤        開発
103       鈴木        総務

=VLOOKUP(102, A1:C4, 2)  // 結果: "佐藤"
=VLOOKUP(102, A1:C4, 3)  // 結果: "開発"
```

## 論理関数

### IF
**説明**: 条件に基づいて異なる値を返します。

**構文**: `=IF(条件, 真の場合の値, 偽の場合の値)`

**例**:
```
=IF(A1>50, "合格", "不合格")       // A1が50より大きければ"合格"
=IF(B1>=100, "達成", "未達成")     // B1が100以上なら"達成"
=IF(C1="", "空白", C1)             // C1が空白なら"空白"と表示
```

**ネストされたIF**:
```
=IF(A1>=90, "優", IF(A1>=70, "良", "可"))
```

### IFERROR
**説明**: 数式がエラーを返す場合に代替値を返します。

**構文**: `=IFERROR(値, エラー時の値)`

**例**:
```
=IFERROR(A1/B1, 0)                    // B1が0の場合、0を返す
=IFERROR(VLOOKUP(A1, B1:C10, 2), "見つかりません")  // VLOOKUPがエラーの場合
=IFERROR(A1*B1, "計算エラー")         // 計算エラー時のメッセージ
```

**使用例**:
```
A列（売上）  B列（目標）  C列（達成率）
1000        800         =IFERROR(A1/B1*100, 0)
500         0           =IFERROR(A2/B2*100, 0)  // 結果: 0（エラー回避）
```

## 実践例

### 例1: 成績表の集計
```
A列（名前）  B列（点数）  C列（評価）
田中        85          =IF(B1>=80, "優", IF(B1>=60, "良", "可"))
佐藤        72          =IF(B2>=80, "優", IF(B2>=60, "良", "可"))
鈴木        55          =IF(B3>=80, "優", IF(B3>=60, "良", "可"))

平均点: =AVERAGE(B1:B3)
合格者数: =COUNTIF(B1:B3, ">=60")
優秀者数: =COUNTIF(C1:C3, "優")
```

### 例2: 売上分析
```
A列（商品）  B列（売上）  C列（目標）  D列（達成率）
商品A       1200        1000        =IFERROR(B1/C1*100, 0)
商品B       800         1000        =IFERROR(B2/C2*100, 0)
商品C       1500        1200        =IFERROR(B3/C3*100, 0)

合計売上: =SUM(B1:B3)
達成商品数: =COUNTIF(D1:D3, ">=100")
目標達成売上: =SUMIF(D1:D3, ">=100", B1:B3)
```

### 例3: 商品マスタ検索
```
マスタ表（A1:C10）:
A列（商品コード）  B列（商品名）  C列（価格）
P001              りんご        150
P002              みかん        100
P003              バナナ        200

注文表:
E列（コード）  F列（商品名）        G列（価格）
P002          =VLOOKUP(E1, A:C, 2)  =VLOOKUP(E1, A:C, 3)
P001          =VLOOKUP(E2, A:C, 2)  =VLOOKUP(E2, A:C, 3)
```

## ヒントとベストプラクティス

1. **範囲指定**: 範囲は常に `A1:B10` のようにコロン（:）で区切ります
2. **文字列**: 文字列を条件として使用する場合は、ダブルクォート（"）で囲みます
3. **エラー処理**: VLOOKUP や除算を使用する場合は、IFERROR でエラーを処理することを推奨します
4. **パフォーマンス**: 大きな範囲で COUNTIF や SUMIF を使用する場合、処理に時間がかかることがあります
5. **数式の確認**: 数式を入力後、Enter キーで確定すると計算結果が表示されます

## トラブルシューティング

### エラーメッセージ
- `#ERROR!`: 数式の構文エラーまたは計算エラー
- `#N/A`: VLOOKUP で値が見つからない場合
- `ERROR`: 一般的なエラー

### よくある問題
1. **範囲が正しくない**: コロン（:）を使用して範囲を指定してください
2. **引数の数が間違っている**: 各関数の必須パラメータを確認してください
3. **データ型の不一致**: 数値と文字列を混在させないでください

## Made with Bob